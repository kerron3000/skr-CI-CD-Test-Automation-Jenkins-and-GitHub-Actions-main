pipeline {
    agent any

    stages {
        stage('unit-test-backend') {
            steps {
                dir('bugtracker-backend') {
                    sh 'export PATH=$PATH:/usr/local/go/bin && go test -v ./...'
                }
            }
        }
    }
}