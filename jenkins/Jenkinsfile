pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
                sh 'echo "Hello Jenkins"'
                sh 'echo "This is the test build 02"'
                sh 'date'
            }
        }
    }
}